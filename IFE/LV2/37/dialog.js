function() {
	var created = [];
	function createElement(nodeconf) {
		document.createElement(nodecond);
	}

	function dialog(config) {
		this.config = config;
		this.tag = createElement({
			tag: "div",
			className: config.target; 
		});
		this.header = createElement({

		})
	}
}